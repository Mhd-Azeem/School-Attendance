import {useState,type FormEvent} from 'react'
import {Navigate} from 'react-router-dom'
import {useAuth} from '../AuthContext'
export function Login(){const {session,login}=useAuth();const [username,setUsername]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false)
 if(session)return <Navigate to="/" replace/>
 async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await login(username.trim(),password)}catch(e){setError(e instanceof Error&&e.message==='invalid_credentials'?'Invalid username or password.':'Could not sign in. Please check your connection and try again.')}finally{setBusy(false)}}
 return <div className="login-page"><section className="login-card"><img className="crest-logo" src="zahira-logo.jpg" alt="Zahira College Matale"/><h1>School Attendance</h1><p>Sign in to your secure portal.</p><form onSubmit={submit}><label>Username<input required value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username" autoCapitalize="none"/></label><label>Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/></label>{error&&<div className="error">{error}</div>}<button className="primary" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form><small className="login-help">Use the username provided by the Section Head.</small></section></div>}
