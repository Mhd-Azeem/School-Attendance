import {useState,type FormEvent} from 'react'
import {Navigate} from 'react-router-dom'
import {LockKeyhole,UserRound} from 'lucide-react'
import {useAuth} from '../AuthContext'
export function Login(){const {session,login}=useAuth();const [username,setUsername]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false)
 if(session)return <Navigate to="/" replace/>
 async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await login(username.trim(),password)}catch(e){setError(e instanceof Error&&e.message==='invalid_credentials'?'Invalid username or password.':'Could not sign in. Please check your connection and try again.')}finally{setBusy(false)}}
 return <div className="login-page"><div className="login-brand"><img src={`${import.meta.env.BASE_URL}zahira-logo.jpg`} alt="Zahira College Matale"/><h1>School Attendance</h1><p>Simple · Smart · Reliable</p></div><section className="login-card"><form onSubmit={submit}><label><span>Username</span><div className="input-icon"><UserRound size={17}/><input required value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username" autoCapitalize="none" placeholder="Enter your username"/></div></label><label><span>Password</span><div className="input-icon"><LockKeyhole size={17}/><input type="password" required value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" placeholder="Enter your password"/></div></label>{error&&<div className="error">{error}</div>}<button className="primary" disabled={busy}>{busy?'Signing in…':'Login'}</button></form></section><small className="login-credit">Developed by Mohammed Azeem ©</small></div>}